# STM32 OTA 固件升级 学习笔记

> 配套项目：`D:\develop\stm32-ota固件升级`（STM32F103C8T6 + Keil MDK + 标准外设库）
> 用途：秋招嵌入式项目复习 + 面试考点速查。所有代码行号均指向项目内真实文件。

---

## 目录

- [第 1 章 全景导览：这个项目在做什么](#第-1-章-全景导览这个项目在做什么)
- 第 2 章 阶段1+1.1：串口底层（DMA+空闲中断）与分区链接（待学习）
- 第 3 章 阶段2：AT24C02 与升级元数据（待学习）
- 第 4 章 阶段3：W25Q64 固件暂存区（待学习）
- 第 5 章 阶段4：内部 Flash 擦写（待学习）
- 第 6 章 阶段5：AB 分区与 Bootloader 跳转原理（待学习）
- 第 7 章 阶段6：完整串口 IAP 与 Xmodem 协议（待学习）
- 第 8 章 面试考点汇总与工程完善方向（待学习）

---

# 第 1 章 全景导览：这个项目在做什么

## 1.1 三个概念辨析（面试常问）

| 概念 | 全称 | 含义 | 关系 |
|---|---|---|---|
| **Bootloader** | 引导程序 | 芯片上电后**最先运行**的一段程序，负责决定"运行谁"以及"要不要先更新固件" | 是 IAP 的载体 |
| **IAP** | In-Application Programming | 在应用编程：**通过预留的通信接口（串口/USB/网络）自己给自己刷固件**，不依赖烧录器 | 是 OTA 的基础 |
| **OTA** | Over-The-Air | 特指**通过无线方式**（WiFi/蓝牙/4G）远程升级固件 | 本质是"无线传输 + IAP" |

三者关系一句话：**Bootloader 是机制，IAP 是"用通信口升级"的能力，OTA 是"用无线上升级"的应用形态**。本项目用的是串口 IAP（Xmodem 协议），但分区、跳转、搬运这套机制与 WiFi 版 OTA 完全相同——换掉传输层就是真 OTA。

## 1.2 硬件组成与各器件角色

MCU：**STM32F103C8T6**（Cortex-M3，Flash 64KB @ 0x08000000，RAM 20KB @ 0x20000000，**Flash 页大小 1KB**，共 64 页）。

| 器件 | 接口 | 在 OTA 中的角色 |
|---|---|---|
| STM32 内部 Flash（64KB） | — | 分成 B 区（Bootloader）+ A 区（App），**程序真正运行的地方** |
| AT24C02（EEPROM，256B~2KB） | 软件 IIC | 存**升级元数据**：升级标志、各固件长度、版本号（掉电不丢） |
| W25Q64（SPI Flash，8MB） | 软件 SPI | **固件仓库/暂存区**：新固件先落到这里，再搬运进内部 Flash |
| USART1（9600, DMA+空闲中断） | 串口 | 固件传输通道（Xmodem）+ 命令行交互 |
| OLED / 按键 / LED | IIC/GPIO | 显示与辅助，不参与升级核心流程 |

为什么需要两个外部芯片？
- **AT24C02 存"要不要升级"**：这个决定必须掉电后仍有效（升级=改程序，程序一重启就全没了，RAM 里的东西活不过复位），所以要放 EEPROM。
- **W25Q64 存"升级的内容"**：新固件不能直接下进内部 Flash 里 App 正在运行的位置（自己擦自己 = 崩溃），也不能放 RAM（20KB 装不下也掉电即失），所以先落外部 Flash。

## 1.3 Flash 分区图（本项目最核心的一张图）

定义在 `6-串口IAP功能\User\main.h:4-10`：

```
内部 Flash 64KB（0x08000000 ~ 0x0800FFFF，64 页 × 1KB）
┌─────────────────────────────────────────────┐
│ B 区：Bootloader  20KB                       │
│ 0x08000000 ~ 0x08004FFF（第 0~19 页）         │  ← 上电永远从这里运行
│   含：跳转逻辑、Xmodem 收包、命令行、搬运代码    │
├─────────────────────────────────────────────┤
│ A 区：App（应用程序）44KB                     │
│ 0x08005000 ~ 0x0800FFFF（第 20~63 页）        │  ← 用户程序住这里
│   被升级/擦除/覆盖的区域                      │
└─────────────────────────────────────────────┘
```

配套的外部存储划分：

```
W25Q64 8MB = 64 个块 × 64KB
  块 0      → OTA 专用固件暂存（收到升级标志后从这里搬运）
  块 1 ~ 9  → 手动固件仓库（命令 5/6 操作，可存多份固件备用）

AT24C02（保存 OTA_InfoCB 结构体，main.h:23-29）
  OTA_Flag  (4B)  == 0x0a050301 时表示"需要 OTA 更新 A 区"
  FileLen[11]     每块的固件长度（字节），[0] 对应 W25Q64 块 0
  OTA_Ver[32]     版本字符串，格式 VER-x.y.z-yyyy/mm/dd-hh:mm
```

## 1.4 七个工程的递进关系（学习路线本身）

| 目录 | 学什么 | 在最终系统中的角色 |
|---|---|---|
| `1-串口测试程序` | USART1 + DMA + 空闲中断接收 | B 区程序的雏形（串口收包底层） |
| `1.1-(A区)串口测试程序` | **同一个程序改链接地址**（IROM 改 0x08005000 + 向量表偏移 0x5000） | A 区 App 的雏形，理解"程序怎么搬到非默认地址还能跑" |
| `2-IO模拟IIC驱动24C02` | 软件 IIC 时序、EEPROM 页写 | 升级元数据的存储层 |
| `3-IO模拟SPI驱动W25Q64` | 软件 SPI 时序、页编程/块擦除 | 固件暂存区的存储层 |
| `4-内部FLASH擦写` | MyFLASH：页擦除、字编程 | "把新固件写进 A 区"的执行层 |
| `5-规划AB分区，第一阶段工程结束` | 分区宏 + 启动分支 + LOAD_A 跳转 + W25Q64→A 区搬运 | Bootloader 骨架（⚠ 有一个已知 bug，见第 6 章） |
| `6-串口IAP功能` | + Xmodem-CRC 收包 + 7 命令菜单 | **最终完整形态**，学习主战场 |

递进逻辑：**先把每一块砖烧好（阶段1~4 的驱动），再砌墙（阶段5 的分区与跳转），最后开门（阶段6 的串口升级入口）**。这是写真实 Bootloader 的标准工作顺序——每个驱动都单独验证过，最后组装才不会一堆问题混在一起。

## 1.5 一次完整升级的全流程（对照代码）

```
上电
 │
 ▼
main() 初始化：OLED / IIC / 串口 / 读AT24C02 / W25Q64   [main.c:21-25]
 │
 ▼
BootLoader_Branch()                                    [boot.c:15-33]
 ├─ BootLoader_Enter(50)：5 秒内收到 'w' ？            [boot.c:36-48]
 │    ├─ 是 → 进入命令行（7 条命令，见 1.6）
 │    └─ 否 ↓
 ├─ OTA_Flag == 0x0a050301 ？（读了 AT24C02 的标志）
 │    ├─ 是 → 置 UpData_A_Flag，源=W25Q64 块 0
 │    │        └─ 主循环搬运：擦 A 区 44 页 → 按页从 W25Q64 读出写进 A 区
 │    │           → 清 OTA_Flag 写回 AT24C02 → NVIC_SystemReset()  [main.c:59-88]
 │    │              重启后再次走分支，此时标志已清 → 跳转 A 区（新固件！）
 │    └─ 否 → LOAD_A(0x08005000) 直接跳 App            [boot.c:262-275]
 │             校验栈顶指针合法 → 设 MSP → 取复位向量
 │             → 反初始化外设 → 函数指针跳转
 ▼
A 区 App 开始运行（其向量表在 0x08005000，链接时就定好了）
```

**关键理解**：升级过程 = "W25Q64 块0 → 内部 Flash A 区"的一次拷贝 + 一个标志位的设置/清除。所有花哨的协议（Xmodem）只是为了把固件**送到** W25Q64；所有跳转代码只是为了让 CPU 在重启后**走对地方**。

## 1.6 Bootloader 命令行（阶段 6 的 7 条命令）

| 命令 | 功能 | 涉及存储 |
|---|---|---|
| 1 | 擦除 A 区 | 内部 Flash |
| 2 | Xmodem 串口 IAP：固件直接写进 A 区 | 内部 Flash |
| 3 | 设置版本号（26 字节固定格式） | AT24C02 |
| 4 | 查询版本号 | AT24C02 |
| 5 | Xmodem 向 W25Q64 块 1~9 下载固件 | W25Q64 + AT24C02(记长度) |
| 6 | 用 W25Q64 块 1~9 的固件更新 A 区 | W25Q64 → 内部 Flash |
| 7 | 复位 | — |

## 1.7 当前实现的闭环缺口（学习时带着这个问题看）

本项目有一个**故意留下的缺口**：全工程搜不到任何代码把 `OTA_Flag` 写成 `OTA_SET_FLAG (0x0a050301)`，也没有代码向 W25Q64 **块 0** 写固件（命令 5 只允许写块 1~9）。

也就是说"App 检测到新版本 → 写升级标志 → 存固件到块 0 → 重启"这条**触发链**还没有实现，是教学留白（真实产品中由 App 或上位机完成）。这也是第 8 章"完善方向"的第一个议题。

## 1.8 阶段 6 关键文件索引（学习主战场）

| 文件 | 干什么 |
|---|---|
| `User/main.h` | 分区宏、状态标志位、OTA_SET_FLAG、OTA_InfoCB / UpDataA_CB 结构体 |
| `User/main.c` | 初始化、主循环（命令分发、'C' 握手、W25Q64→A 区搬运） |
| `Hardware/boot.c` | 启动分支、5 秒窗口、命令处理、Xmodem 收包、CRC16、LOAD_A 跳转 |
| `Hardware/MyFLASH.c` | 内部 Flash 页擦除 + 字编程 |
| `Hardware/usart.c` | USART1 9600 + DMA + 空闲中断 + 环形缓冲，自实现 printf |
| `Hardware/m24c02.c` + `iic.c` | AT24C02 读写（OTA 信息存取） |
| `Hardware/W25Q64.c` + `MySPI.c` | W25Q64 驱动（读 ID/页编程/扇区擦/64K 块擦/读数据） |

---
